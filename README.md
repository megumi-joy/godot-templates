# godot-templates

Size-trimmed Godot Android export templates (arm64 only), built from source on 182 with `build_template.sh`.
Each release tag = `<godot-tag>-min`, assets `android_release.apk` + `android_source.zip`; CI drops them over the stock template in `~/.local/share/godot/export_templates/<ver>.stable/`.
